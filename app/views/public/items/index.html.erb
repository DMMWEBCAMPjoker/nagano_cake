<!--<div class='col-md-8 offset-md-1'>-->
  <!--<div class='row'>-->
  <div>
<h2>商品一覧(全<%=@items.count %>件)</h2>
  </div>

  <% @items.each do |item| %>
  <div>
  <%= image_tag item.image %>
  <p><%= link_to item.name %></p>
  <p>¥<%= item.price %></p>
  </div>
  <% end %>