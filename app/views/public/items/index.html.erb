<div class="container">
  <div class="row">
    <div class="col-md-2">
      <h4>ジャンル検索</h4>
    </div>

    <div class='col-md-10'>
      <div>
        <h4>商品一覧(全<%=@items.count %>件)</h4>
      </div>

  <!--<#% @items.each do |item| %>-->

  <!--<div class="col-sm-4">-->
  <!--  <div class="row row-cols-2">-->
    <!--<div class="d-flex justify-content-start">-->
  <!--  <#%= image_tag item.get_item_image(200, 200) %><br>-->
  <!--<p><#%= link_to item.name, items_show_path(item.id) %></p><br>-->
  <!--<p>¥<#%= item.price.to_s(:delimited) %></p><br>-->
  <!--</div>-->
  <!--</div>-->
<div class="row mt-3 mb-3 text-center text-xs-center text-sm-center text-md-left">
<div class="d-flex flex-wrap-auto">
<!--<div class="col-md-6 col-lg-3">-->
   <% @items.each do |item| %>
   <div class="d-flex flex-column">
     <%= image_tag item.get_item_image(200, 200) %>
      <p><%= link_to item.name,items_show_path(item.id) %></p>
      <p>¥<%= item.price.to_s(:delimited) %></p>
       </div>
       <% end %>
       </div>
       </div>

  <!--<#% end %>-->
  </div>
<%= paginate @items %>
</div>
</div>
</div>