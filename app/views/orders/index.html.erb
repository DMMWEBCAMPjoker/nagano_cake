<div class="container">
  <div class="row my-3">
    <div class="col-sm-10 px-sm-0 offset-sm-1">
      <h4  class="table-active my-3" style="width:16%">注文履歴一覧</h4>
      </div>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th class="table-active" style="width:12%">注文日</th>
            <th class="table-active" style="width:35%">配送先</th>
            <th class="table-active" style="width:20%">注文商品</th>
            <th class="table-active" style="width:10%">支払金額</th>
            <th class="table-active" style="width:10%">ステータス</th>
            <th class="table-active" style="width:12%">注文詳細</th>
          </tr>
        </thead>
        <% @orders.each do |order| %>
        <tbody>
          <tr>
            <td><%= order.created_at.strftime('%Y/%m/%d') %></td>
            <td>〒<%= order.delivery_postcode %><br>
                  <%= order.delivery_address %><br>
                  <%= order.delivery_name %>
            </td>
            <td><% order.order_items.each do |order_item| %>
              <%= order_item.item.name %>
              <br>
              <% end %>
            </td>
            <td><%= order.invoice - order.postage %>円</td>
            <td><%= order.status_i18n %></td>
            <td><%= link_to "表示する", order_path(order.id), class:"btn btn-primary" %></td>
          </tr>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>